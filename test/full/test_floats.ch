// full pass

fn main() -> Int {
    let x = 1.0.
    let y = 1.5.

    println("x = \(x)").
    println("y = \(y)").
    println("x + y = \(x + y)").
    println("x - y = \(x - y)").
    println("x * y = \(x * y)").
    println("x / y = \(x / y)").
    println("x == y = \(x == y)").
    println("x < y = \(x < y)").
    println("x > y = \(x > y)").
    println("x <= y = \(x <= y)").
    println("x >= y = \(x >= y)").
    println("1 as Float = \(1 as Float)").

    0
}