// export fn foo(a: Int, b: Int).

trait Bar<_T> {
    type Baz.

    fn bing() -> <Self as Bar<_T>>::Baz.
}

impl Bar<Int> for Int {
    type Baz = Int.

    fn bing() -> Int { 2 }
}

fn main() -> Int {
    -(1)
}
